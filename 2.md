
## 변경 내용
2단계로 넘어 오면서 수정된 내용 md
1. **`FridgeScreen.js`** — `↔ useFetch.js:줄번호` 대응 주석 추가, 제목 → "2단계 — useFetch와 비교"
2. **`hooks/useFetch.js`** — `↔ FridgeScreen.js:줄번호` 역방향 주석 추가
3. **`App.js`** — 상단에 "2단계" 배너 표시

## 2단계 학습 방법

1. **`FridgeScreen.js`** 와 **`hooks/useFetch.js`** 를 **나란히** 열기
2. `↔` 주석 따라 **같은 줄끼리** 매칭
3. **차이 3가지만** 확인:
   - `items` → `data` (이름만)
   - `FRIDGE_URL` → `url` (인자로 받음)
   - `return JSX` → `return { data, loading, error }`
   - `[]` → `[url]` (URL 바뀌면 재fetch)

## 참고

2단계는 **코드 토글 없이 비교만** 하는 단계입니다.  
비교가 끝나면 **3단계**로 넘어가면 됩니다 — 말씀해 주시면 진행하겠습니다.
---

### 차이 정리 (핵심 4가지)

| | FridgeScreen | useFetch |
|--|--------------|----------|
| **데이터 이름** | `items` / `setItems` | `data` / `setData` |
| **URL** | `FRIDGE_URL` 상수 고정 | `url` **인자**로 받음 |
| **반환** | JSX (화면) | `{ data, loading, error }` |
| **의존성** | `[]` — 마운트 1번 | `[url]` — url 바뀌면 **다시 fetch** |

---

### 40~44줄 (거의 동일)

- `doFetch()` ↔ `fetchItems()` — **이름만** 다름, 역할 같음
- `cancelled = true` — **완전 동일**
- `[url]` vs `[]` — **여기가 실질 차이** (재사용 훅이라 URL이 바뀔 수 있음)

---

### 나머지는 **동일**

fetch, `cancelled`, `loading`/`error` 처리 흐름은 **복사한 것**과 같다고 보면 됩니다.

**2단계 목표:** "긴 fetch 코드 = useFetch 안에 들어간 것" + 위 4가지 차이 이해 → **3단계**에서 FridgeScreen이 짧아지는 이유가 보이면 OK입니다.



인계문
# useFetch 학습 프로젝트 — 인계문

## 프로젝트 개요

- **경로:** `/Users/apple/Documents/device_claude_design_alltab/260610`
- **목적:** `FridgeScreen` 안 fetch 로직 → `useFetch` 커스텀 훅 추출 → 화면별 재사용 학습
- **스택:** Expo 56 (blank template), React Native
- **실행:** `npm start` + Expo Go (에뮬레이터 불필요, 빌드 불필요)
- **Remote:** `gimsansan/260610_useFet_icedrum` — push 시 `gh auth status`로 active가 `gimsansan`인지 확인

---

## Git 상태

| 커밋 | 메시지 |
|------|--------|
| `99edea3` | `2stage_hikaku` ← **현재 HEAD** |
| `7f73ebc` | `first` |
| `d8edce1` | `Initial commit` |

- `main` = `origin/main` 동기화됨
- **다음 단계(3·4) 작업은 아직 커밋 전** — Agent 모드에서 진행 후 커밋 여부는 사용자 판단

---

## 파일 구조

```
260610/
├── App.js                    # 탭(냉장고/드럼) + 2단계 배너
├── screens/
│   ├── FridgeScreen.js       # ★ 1단계 fetch 활성 / 3단계 주석 대기
│   └── DrumScreen.js         # placeholder / 4단계 주석 대기
├── hooks/
│   └── useFetch.js           # 훅 본문 + ↔ 대응 주석
├── components/
│   ├── FridgeItemList.js
│   └── DrumSoundList.js
├── utils/
│   └── mockApi.js            # api.example.com → mockFetch
└── 2.md                      # 사용자 2단계 학습 노트
```

---

## 학습 단계 진행 현황

| 단계 | 상태 | 내용 |
|------|------|------|
| **1단계** | ✅ 완료 | `FridgeScreen` 안 fetch + state 3개 + cancelled |
| **2단계** | ✅ 진행 중/완료 | `FridgeScreen.js` ↔ `useFetch.js` 비교 (`↔` 줄번호 주석) |
| **3단계** | ⬜ **다음** | `FridgeScreen` 주석 토글 → `useFetch` 사용 |
| **4단계** | ⬜ 대기 | `DrumScreen` 주석 토글 → 훅 재사용 |

---

## 2단계에서 익힌 차이 (4가지)

| | FridgeScreen | useFetch |
|--|--------------|----------|
| 데이터 이름 | `items` | `data` |
| URL | `FRIDGE_URL` 상수 | `url` 인자 |
| 반환 | JSX | `{ data, loading, error }` |
| useEffect deps | `[]` | `[url]` |

나머지 fetch / cancelled / loading·error 흐름은 **동일**.

---

## 3단계 — Agent에게 요청할 작업

**파일:** `screens/FridgeScreen.js`

1. **주석 해제:** `import useFetch from '../hooks/useFetch'`
2. **주석 처리:** 15~59줄 (2단계 `function FridgeScreen` 전체)
3. **주석 해제:** 65~77줄 (3단계 `function FridgeScreen`)
4. **import 정리:** `useEffect`, `useState`, `isMockUrl`, `mockFetch` 제거 (3단계에 불필요)
5. **App.js** 배너 → "3단계"로 업데이트

**확인:** 앱 제목 "냉장고 (3단계 — useFetch)", 로딩 → 목록 정상

---

## 4단계 — 3단계 완료 후

**파일:** `screens/DrumScreen.js`

1. **주석 해제:** `import useFetch`
2. **주석 해제:** 11~23줄 (4단계 `function DrumScreen`)
3. **주석 처리/삭제:** 25~34줄 (placeholder)
4. **App.js** 배너 → "4단계"로 업데이트

**확인:** 드럼 탭 → 로딩 → 킥/스네어/하이햇 목록

---

## 핵심 개념 (새 채팅에서 참고)

- **`loading` + `!cancelled`:** 부모가 로딩 중 리스트에 `null` 안 넘김; 자식 `FridgeItemList`는 `!items` 2차 방어
- **`cancelled`:** `false`=화면 살아있음, `true`=나감 → `!cancelled`일 때만 setState
- **mockApi:** `api.example.com`은 연습용 → `mockFetch`, 실제 URL은 `fetch`
- **push:** 학습에 필수 아님; 필요 시 `gh auth switch`로 active `gimsansan` 확인 (`user.name` ≠ push 계정)

---

## 새 채팅 시작 프롬프트 (복사용)

```
@/Users/apple/Documents/device_claude_design_alltab/260610
useFetch 학습 프로젝트 이어서 진행.
현재 2단계 완료, 3단계(FridgeScreen useFetch 전환)부터 Agent로 진행해줘.
인계: FridgeScreen 1단계 코드 활성 → 3단계 주석 토글, App.js 배너 업데이트.
3단계 확인 후 4단계(DrumScreen) 진행.
```

---

**다음 액션:** 새 채팅에서 위 프롬프트 붙여넣기 → **3단계**부터 진행하면 됩니다.